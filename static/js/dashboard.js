// Dashboard JS (barebones for now)
document.addEventListener('DOMContentLoaded', function () {
  console.log("Chuck E Sync Custom Calendar Loaded");
  
  // Eventually we will load shifts here via your existing /api/shifts
});
